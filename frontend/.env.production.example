# Frontend PRODUCTION env template. Copy to `frontend/.env.production` (or set
# the value in the build environment) before `npm --prefix frontend run build`.
#
#     cp frontend/.env.production.example frontend/.env.production
#
# VITE_* vars are baked into the bundle at BUILD time — changing this requires
# a fresh build + redeploy of frontend/dist.
# !! COMMITTED — placeholders only. !!

VITE_API_URL=https://hireorbitai.com/api
