# Frontend env vars. Anything prefixed with VITE_ gets bundled into the
# client at build time — DO NOT put server-only secrets here.
#
# These are *baked into the static bundle*. Changing them requires re-running
# `npm run build` and redeploying the dist/ directory.
VITE_API_URL=https://hireorbitai.com/api
